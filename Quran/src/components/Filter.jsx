function Filter({}) {
    use
  return (
    <div>
      <select
        className=" px-5 py-3 border border-stone-900 rounded-md bg-stone-100 h-fit outline-none max-w-[30%]"
        name="langs1"
        id="langs1"
      >
        <option value="">sdds</option>
        <option value="">sd1ds</option>
        {/* {langs.map((l, i) => (
          <option value={i} key={i}>
            {l}
          </option>
        ))} */}
      </select>
    </div>
  );
}

export default Filter;
